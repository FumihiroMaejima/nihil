# GameEnemies:
  type: array
  items:
    type: object
    properties:
      id:
        type: integer
        format: int64
      name:
        type: string
      level:
        type: integer
        format: int64
      hp:
        type: integer
        format: int64
      mp:
        type: integer
        format: int64
      offece:
        type: integer
        format: int64
      defense:
        type: integer
        format: int64
      speed:
        type: integer
        format: int64
      magic:
        type: integer
        format: int64
